{
  "name": "phronesis",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "migrate": "node --env-file-if-exists=.env.local scripts/migrate.ts",
    "migrate:dev": "node --env-file-if-exists=.env.development.local scripts/migrate.ts"
  },
  "dependencies": {
    "@vercel/functions": "^2.0.0",
    "aws4fetch": "^1.0.20",
    "next": "^15.3.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
